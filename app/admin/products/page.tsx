"use client";
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {AdminShell} from '../../components'; import {apiFetch} from '../../lib';
export default function AdminProducts(){const[products,setProducts]=useState<any[]>([]);const[q,setQ]=useState('');useEffect(()=>{apiFetch('/api/products').then(r=>r.json()).then(d=>setProducts(d.products||[]))},[]);const list=products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase()));return <AdminShell><div className="topbar"><div><span className="eyebrow">Catalog management</span><h1>Products</h1></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search 100 products..."/></div><section className="panel"><div className="panel-head"><div><span className="eyebrow">Fixed catalog</span><h2>100 products available for orders</h2></div><span className="admin-chip">Prices are order amounts</span></div><div className="product-grid admin-product-grid">{list.map(p=><Link href={`/admin/products/${p.id}`} className="product-card" key={p.id}>{p.image && <img src={p.image} alt={p.name}/> }<div className="product-body"><h3>{p.name}</h3><strong>${p.price.toLocaleString()}</strong></div></Link>)}</div></section></AdminShell>}
