"use client";
import {useEffect,useState} from "react"; import {AdminShell} from "../../components"; import {apiFetch} from "../../lib";
export default function Users(){const[users,setUsers]=useState<any[]>([]);const[search,setSearch]=useState("");const[error,setError]=useState("");const load=()=>apiFetch("/api/admin/users",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setUsers(d.users)}).catch(e=>setError(e.message));useEffect(()=>{load()},[]);const toggle=async(u:any)=>{const status=u.status==="Active"?"Suspended":"Active";const r=await apiFetch(`/api/admin/users/${u.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});if(r.ok)load();else{const d=await r.json();setError(d.error||"Could not update user")}};const remove=async(u:any)=>{if(!confirm(`Delete ${u.name}? This removes the customer account and its orders.`))return;const r=await apiFetch(`/api/admin/users/${u.id}`,{method:"DELETE"});if(r.ok)load();else{const d=await r.json();setError(d.error||"Could not delete user")}};const filtered=users.filter(u=>(u.name+u.email).toLowerCase().includes(search.toLowerCase()));return <AdminShell><div className="topbar"><div><span className="eyebrow">Management</span><h1>Users</h1></div><input className="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search users..."/></div>{error&&<div className="form-error">{error}</div>}<section className="panel"><div className="panel-head"><div><span className="eyebrow">{filtered.length} visible</span><h2>Real local accounts</h2></div><span className="admin-chip">Database connected</span></div><div className="table-wrap"><table><thead><tr><th>User</th><th>Status</th><th>Created</th><th>Action</th></tr></thead><tbody>{filtered.map(u=><tr key={u.id}><td><div className="user-cell"><span className="avatar">{u.name[0]}</span><div><b>{u.name}</b><small>{u.email}</small></div></div></td><td><span className={`status ${u.status.toLowerCase()}`}>{u.status}</span></td><td>{new Date(u.createdAt).toLocaleDateString()}</td><td>{u.role==="admin"?<span className="table-btn">Admin</span>:<div className="action-row"><button className="table-btn" onClick={()=>toggle(u)}>{u.status==="Active"?"Suspend":"Activate"}</button><button className="table-btn danger-btn" onClick={()=>remove(u)}>Delete</button></div>}</td></tr>)}</tbody></table></div></section></AdminShell>}
