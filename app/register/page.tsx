"use client";
import Link from "next/link"; import {FormEvent,useEffect,useState} from "react"; import {useRouter} from "next/navigation"; import {saveSession} from "../lib"; import {Logo} from "../components"; import {countryCodes,getCountry} from "../countries";
export default function Register(){const router=useRouter();const[invite,setInvite]=useState("");const[name,setName]=useState("");const[username,setUsername]=useState("");const[phone,setPhone]=useState("");const[countryCode,setCountryCode]=useState("+1");const[country,setCountry]=useState("United States");const[inviteState,setInviteState]=useState<"checking"|"valid"|"invalid">("invalid");const[inviteError,setInviteError]=useState("");const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[remember,setRemember]=useState(true);const[error,setError]=useState("");const[loading,setLoading]=useState(false);const selected=getCountry(country);
 useEffect(()=>{const token=new URLSearchParams(window.location.search).get("invite")||"";setInvite(token);if(!token){setInviteState("invalid");return;}setInviteState("checking");fetch(`/api/invites/${encodeURIComponent(token)}`,{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok||!d.valid)throw new Error(d.error||"Invalid invitation");setInviteState("valid")}).catch(e=>{setInviteState("invalid");setInviteError(e.message)})},[]);
 const changeCountry=(value:string)=>{const x=getCountry(value);setCountry(value);setCountryCode(x?.code||"+1");setPhone("")};
 const submit=async(e:FormEvent)=>{e.preventDefault();setError("");setLoading(true);try{const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,username,email,phone,countryCode,country,password:pw,remember,inviteToken:invite})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Unable to create account");saveSession(data.user.role,data.user,data.token);router.push("/dashboard");}catch(err){setError(err instanceof Error?err.message:"Unable to create account");}finally{setLoading(false);}};
 return <main className="auth-page"><div className="auth-logo"><Logo/></div><section className="auth-card"><div className="auth-icon">+</div><h1>Create account</h1><p>Start your private Drop Zone workspace.</p>{inviteState!=="valid"&&<div className="form-error">{invite? (inviteState==="checking"?"Checking invitation…":inviteError||"This invitation is not valid."):"An invitation link is required to create an account."}</div>}{inviteState==="valid"&&<div className="info-banner">Invitation accepted <span>This link can be used once.</span></div>}<form className="auth-form" onSubmit={submit}>{error&&<div className="form-error">{error}</div>}<label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required/></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value.toLowerCase())} placeholder="Choose a username" minLength={3} maxLength={24} pattern="[A-Za-z0-9._-]{3,24}" required/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label>Country<select value={country} onChange={e=>changeCountry(e.target.value)}>{countryCodes.map(c=><option key={c.country}>{c.country}</option>)}</select></label><label>Phone number<div style={{display:"flex",gap:8}}><select value={countryCode} disabled style={{width:110}}><option value={countryCode}>{countryCode}</option></select><input value={phone} inputMode="numeric" maxLength={selected?.digits||10} onChange={e=>setPhone(e.target.value.replace(/\D/g,"").slice(0,selected?.digits||10))} placeholder="Phone number" style={{flex:1}} required/></div></label><label>Password<input type="password" value={pw} onChange={e=>setPw(e.target.value)} placeholder="At least 6 characters" minLength={6} required/></label><label className="remember-row"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/><span>Remember me</span></label><button className="btn full" disabled={loading||inviteState!=="valid"} type="submit">{loading?"Creating…":"Create account"}</button></form><p className="switch">Already have an account? <Link href="/login">Sign in</Link></p></section></main>}
