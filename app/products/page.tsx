"use client";
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {UserShell} from '../components'; import {apiFetch} from '../lib';
export default function Products(){const[products,setProducts]=useState<any[]>([]);const[q,setQ]=useState('');useEffect(()=>{apiFetch('/api/products').then(r=>r.json()).then(d=>setProducts(d.products||[]))},[]);const list=products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase()));return <UserShell><div className="topbar"><div><span className="eyebrow">Catalog</span><h1>Top 100 Products</h1></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..."/></div><div className="product-grid">{list.map(p=><Link href={`/products/${p.id}`} className="product-card" key={p.id}>{p.image && <img src={p.image} alt={p.name}/> }<div className="product-body"><h3>{p.name}</h3></div></Link>)}</div></UserShell>}
